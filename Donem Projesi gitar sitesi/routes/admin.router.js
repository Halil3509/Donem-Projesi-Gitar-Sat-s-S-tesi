const mongoose = require('mongoose');

const AdminModel  = require('../models/admin.model');

const express = require('express');
const router = express.Router();


// router.get('/olustur',HomeController.signup);

module.exports = router;